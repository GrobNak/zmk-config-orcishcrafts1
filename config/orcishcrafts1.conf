# Force low-level Nordic chip compilation to release NFC pins
CONFIG_NFCT_PINS_AS_GPIOS=y
# Enable ZMK Studio integration
CONFIG_ZMK_STUDIO=y
CONFIG_USB_DEVICE_STACK=y
# Disable ZMK Studio security locking
CONFIG_ZMK_STUDIO_LOCKING=n

# Core hardware drivers
CONFIG_ZMK_DISPLAY=y
CONFIG_ST7789V=y

# 1. Completely turn off the built-in and custom display rendering engines
# This tells ZMK to skip looking for or compiling status screen C code
CONFIG_ZMK_DISPLAY_STATUS_SCREEN_BUILT_IN=n
CONFIG_ZMK_DISPLAY_STATUS_SCREEN_CUSTOM=n

# 2. Allocate low-level graphic drivers directly through Zephyr/LVGL 
CONFIG_LVGL=y
CONFIG_LV_MEM_CUSTOM=y
CONFIG_LV_Z_MEM_POOL_SIZE=16384
CONFIG_LV_Z_BITS_PER_PIXEL=16
CONFIG_LV_Z_VDB_SIZE=10

# 3. Suppress font loading components that crash color arrays
CONFIG_ZMK_DISPLAY_WORK_QUEUE_DEDICATED=y
