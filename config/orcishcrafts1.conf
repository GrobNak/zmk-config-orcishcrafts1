# Force low-level Nordic chip compilation to release NFC pins
CONFIG_NFCT_PINS_AS_GPIOS=y

# Stop the compiler from aborting when it sees the above legacy flag
CONFIG_KCONFIG_WARNINGS_AS_ERRORS=n

# Stabilize the P1.xx high-drive pin cluster power supply
CONFIG_BOARD_ENABLE_DCDC=y
